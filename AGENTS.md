# Duckle agent instructions

## Spec Kit

This repository uses Spec Kit. The current feature is `specs/003-quack-sidecar-database-runner`; keep its `spec.md`, `plan.md`, and `tasks.md` aligned with the work.

## Formatting

Do not format code, run automatic formatters, or make formatting-only edits. You may check formatting, but leave any required formatting changes to the user unless they explicitly request them.
